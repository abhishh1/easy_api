name: easy_api
description: Best way to handle API connection, Data model parsing and API Exceptions handling.
version: 0.0.1
repository: https://github.com/abhishh1/easy_api

environment:
  sdk: ">=2.16.2 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^0.13.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
